/**
 * Legal / consent links for forms.
 * Replace placeholder URLs when real pages are ready.
 */
export const legalConfig = Object.freeze({
  privacyPolicyUrl: "/privacy",
  privacyPolicyLabel: "политикой обработки персональных данных",
  consentRequiredLabel:
    "Я согласен(на) на обработку персональных данных",
});
